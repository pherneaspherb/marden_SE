import 'package:flutter/material.dart';

class CustomerHistoryPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Order History Page', style: TextStyle(fontSize: 18)),
    );
  }
}
